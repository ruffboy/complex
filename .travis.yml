services:
    - docker


before_install:
    - docker build -t js/react-test -f ./client/Dockerfile.dev ./client

script:
    - docker run js/react-test npm test -- --coverage

after_success:
    - docker build -t js/complex-client ./client
    - docker build -t js/complex-nginx ./nginx
    - docker build -t js/complex-server ./server
    - docker build -t js/complex-worker ./worker
    - echo "$docker_pass" | docker login -u "$docker_id" --password-stdin
    - docker push js/complex-client
    - docker push js/complex-nginx 
    - docker push js/complex-server
    - docker push js/complex-worker